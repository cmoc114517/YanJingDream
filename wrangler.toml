# Cloudflare Pages + Functions 配置
# 这个文件主要用于本地开发和文档，线上部署通过 Cloudflare Dashboard 完成

name = "yanjing-cloudflare"
compatibility_date = "2025-05-26"

# 部署到 Cloudflare Pages 时，
# 请在 Dashboard → Pages → 你的项目 → Settings → Functions → KV namespace bindings 中绑定：
#
# Variable name: USER_DATA
# KV namespace: (你创建的 KV 命名空间)
